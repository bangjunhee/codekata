class Solution {
    fun solution(num1: Int, num2: Int): Int {
        if (num1 in 1 .. 100 && num2 in 1 .. 100){
            var answer: Int = num1 / num2
            return answer
        }
        else{
            return 0
        }
    }
}
