class Solution {
    fun solution(a: IntArray, b: IntArray): Int {
        var answer: Int = 0
        var len = a.size
        for (i in 0 .. len-1){
            answer += a[i]*b[i]
        }
        return answer
    }
}
