class Solution {
    fun solution(n: Int): Int {
        var tempNumber = n
        var sum = 0
        while (tempNumber != 0) {
        sum += tempNumber % 10
        tempNumber /= 10
    }
        return sum
    }
}
