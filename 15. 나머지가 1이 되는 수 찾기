class Solution {
    fun solution(n: Int): Int {
        var nums = arrayOf<Int>()
        for (i in 1 .. n){
            if(n % i == 1)
                nums = nums.plus(i)
        }
        val sortedNums = nums.sorted()
        return sortedNums[0]
    }
}
